extends settings/layout

prepend html
    - const lang = "ru";

prepend head
    - const pageTitle = "";
    - const themeColor = "#ffffff";

append head

append content
        include components/header


append scripts
    script(defer src="js/main.js")
